name: ' Test...'

on:
  workflow_dispatch:
    inputs:
      atest:
        description: abc123
        required: false
        default: 'current'

permissions:
  actions: read
  contents: read
  id-token: write
  pull-requests: read
  checks: read

defaults:
  run:
    shell: powershell

jobs:
  test:
    name: Test write
    runs-on: ubuntu-latest
    steps:
    - name: Use variables
      shell: pwsh
      run: |
        Write-Host "Regular message"  
        Write-Host "::Error::Error message"
        Write-Host "::Warning::Warning message"
        Write-Host "::Notice::Notice message"
        Write-Host "::Debug::Debug message"
